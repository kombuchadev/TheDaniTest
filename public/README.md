Put Dani's photo here as `dani.jpg`.

It is cropped to a circle with object-fit: cover, so anything roughly square
works. Until that file exists the page falls back to a drawn helmet, so a
missing photo never looks broken.
